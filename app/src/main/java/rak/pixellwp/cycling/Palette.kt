package rak.pixellwp.cycling

import android.graphics.Color

class Palette(colors: List<Int>, cycles: List<Cycle>){
    val colors = colors
    val baseColors = colors
    val cycles = cycles
    fun getRawTransformedColors(): List<Int> {return listOf()}


}